@n8n host n8n.{$BASE_DOMAIN}
handle @n8n {
  import default-headers

  reverse_proxy http://{args[0]}:{args[1]} {
    flush_interval -1

    health_uri /healthz
    health_interval 1m
    health_timeout 10s
    health_status 2xx
  }
}
